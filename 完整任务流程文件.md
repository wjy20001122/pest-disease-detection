# 完整任务流程文件（当前版本）

更新时间：2026-05-08

## 阶段A：业务闭环（已完成）
1. 检测能力按角色分流：
   - 普通用户：仅图像检测
   - 管理员：图像/视频/摄像头
2. 图像链路按身份分流：
   - 管理员：手选模型直连
   - 普通用户：AI + 本地知识库约束分析
3. 审查与通知闭环：
   - 高风险触发 -> 审查 -> 通知 -> 误报回写

## 阶段B：知识库与问答闭环（已完成）
1. 知识库迁移 MySQL 与幂等导入。
2. 知识库接口切换 DB 查询并支持筛选元数据。
3. QnA 会话持久化，失败错误态可见可重试。
4. QnA 上下文注入作物/类别约束。

## 阶段C：异步与门禁（已完成）
1. 视频异步升级 Celery + Redis。
2. `video_tasks` 状态持久化，支持停止/失败/完成流转。
3. `predeploy_check` + `pytest` + 回归脚本纳入门禁。

## 阶段D：ESP 本地实时检测（已完成）
1. 新增 `ESP/` 独立项目，不放入 `backend/` 主平台。
2. ESP32-CAM Arduino 固件通过 UDP 发送 JPEG 分包。
3. 小主机 FastAPI 服务监听 UDP `9000`，重组帧后本地 ONNX 推理。
4. FastAPI 通过 `/camera/frame.mjpg` 输出实时检测画面。
5. PySide6 Qt 客户端显示实时画面、FPS、UDP 状态与检测结果。
6. 部署文档、固件说明与本地隐私边界已补齐。


---

## 状态定义
- `todo`：未开始
- `in_progress`：进行中
- `done`：已完成并可交付
