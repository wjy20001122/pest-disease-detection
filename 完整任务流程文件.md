# 完整任务流程文件（按当前项目执行）

更新时间：2026-05-01（知识库工作台与前端壳层已同步）

## 阶段A：业务闭环（已完成）
### A1 检测能力分流
1. 用户端仅保留图像检测入口。
2. 管理员保留图像/视频/摄像头入口。
3. 管理员图像检测支持直连 `model_key` 且不走 AI。

### A2 用户约束闭环
1. 用户图像检测必须提交 `crop_type`、`category`。
2. AI 分析与知识库召回均受用户选择约束。
3. 越界场景返回“与用户选择不一致”提示。

完成判定：同一路由下实现“管理员直连模型 + 用户受约束AI”双通道。

## 阶段B：知识库与问答闭环（已完成）
### B1 知识库 MySQL 化
1. 建立 `knowledge_items` 表并完成种子导入。
2. `knowledge/search`、`recent`、`detail` 全部切换 DB 查询。
3. 新增 `shapes/colors` 过滤元数据接口。
4. 补齐 36 条中国权威来源结构化知识与来源元数据。

### B2 QnA 收口
1. `POST /qna/ask` 支持 JSON Body 主路径并兼容旧 Query。
2. 会话与消息写入 `qna_conversations`、`qna_messages`。
3. 前端请求失败改为明确错误卡片 + 重试，不再返回模拟答案。

完成判定：知识库与问答不依赖内存 mock，全部走持久化链路。

## 阶段B2：前端体验升级（已完成）
### B2.1 知识库工作台
1. 知识库页重做为搜索栏 + 筛选面板 + 结果区 + 详情抽屉。
2. 支持来源筛选、更新时间筛选、卡片/列表双视图。
3. 条目详情展示来源、书名、出版社、年份、章节，并支持复制/跳转。

### B2.2 主壳层滚动模型
1. 左侧导航固定，不随页面内容滚动。
2. 顶栏固定，主体内容区独立滚动。
3. 平板/移动端筛选面板改为抽屉式交互。

完成判定：桌面与移动端均保持“导航固定、主体滚动”的统一体验。

## 阶段C：交付门禁（已完成）
### C1 代码与构建验证
1. 后端编译检查通过（`compileall`）。
2. 前端构建通过（`npm run build`）。

### C2 全量回归与部署前验收
1. 执行 `predeploy_check.py`（DB/Redis/OSS/API）。
2. 执行回归脚本集与 `pytest --tb=short`。
3. 增加 Playwright 前端 E2E 并复跑到全绿。

完成判定：脚本全绿 + 构建通过 + CI 阻断链路有效（2026-04-30 已验收）。

## 阶段D：发布闭环（进行中）
### D1 双通道发布
1. 宝塔脚本直发链路保持可用。
2. GitHub Actions 测试与部署链路保持阻断语义。
3. 本地与 CI 测试命令保持一致。

### D2 最后待办（按你的要求延后）
1. 在仓库 Secrets 中补齐 `SERVER_HOST`、`SERVER_PORT`、`SERVER_USER`、`SERVER_PASSWORD`。
2. 推送一次代码触发 Actions，确认测试与部署全绿。
3. 记录发布 SHA 与回滚 SHA。

完成判定：补齐 Secrets 后，Actions 成功完成一次完整部署。

## 状态流转规范
- `todo`：待开始
- `in_progress`：开发或联调中
- `done`：通过验收并可发布
