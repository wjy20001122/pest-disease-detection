# 完整任务描述文件（按项目模块）

更新时间：2026-04-30（阶段D收尾中）

## 1. 用户与认证模块
### 已完成
- 后端：注册、登录、登出、用户资料更新。
- 前端：登录/注册页、token 注入、路由守卫。
- 权限：`requiresAdmin` 管理后台访问控制。
### 待完成
- 将权限拒绝、账号锁定、敏感操作的审计日志在后台可视化展示。

## 2. 检测核心模块
### 已完成
- 图像检测：`POST /detection/image` 全链路可用。
- 视频检测：`POST /detection/video` + Celery 异步 + `video_tasks` 状态持久化。
- 摄像头检测：`camera/start/stream/stop` + `camera/ws`。
- 模型策略：`model_policies` 控制默认模型、启停、云端回退。
- 用户端能力收敛：前端仅暴露图像检测。
- 管理员能力保留：图像/视频/摄像头均可使用。
- 管理员图像检测：支持 `model_key` 直连本地模型，不走 AI 路由。
- 用户图像检测：强制 `crop_type` 与 `category`，并对 AI 与结果应用范围约束。
### 待完成
- 无（该模块本轮计划项已完成）。

## 3. 审查与通知模块
### 已完成
- 检测命中条件时触发 `review_agent`。
- 预警/区域预警通知落库并可读。
- 误报事件落库（`review_events`）。
- 审查阈值、区域告警阈值改为后台配置读取并支持默认回退。
### 待完成
- 无（该模块本轮计划项已完成）。

## 4. 跟踪、历史、导出模块
### 已完成
- 跟踪任务创建、更新、删除、列表与详情。
- 历史记录分页查询与详情。
- 检测数据导出接口。
### 待完成
- 大数据量场景的历史筛选查询性能压测与优化。

## 5. 统计分析模块
### 已完成
- 用户侧统计：`/detection/stats` 与 `/detection/stats/overview`。
- 前端统计页图表展示。
### 待完成
- 管理端统计接口统一（前端 `adminApi.stats` 与后端路由需对齐）。

## 6. 知识库与问答模块
### 已完成
- 知识库：迁移到 MySQL（`knowledge_items`），支持搜索/最近更新/详情/管理端查询。
- 知识库：新增 `shapes/colors` 元数据接口，支持前端筛选。
- 数据范围：仅保留小麦/玉米/水稻种子数据，并支持幂等导入。
- 问答：提问、会话列表、会话详情已改为 DB 持久化（`qna_conversations`、`qna_messages`）。
- 问答：AI 调用注入用户筛选上下文（作物/类别）并遵守约束。
### 待完成
- 无（该模块本轮计划项已完成）。

## 7. 管理后台模块
### 已完成
- 概览、用户、通知、模型策略页面与后端接口。
- `/admin/knowledge` 已切换为真实数据库数据。
- 权限审计页与系统配置页已接入主后台路由。
### 待完成
- `admin/Dashboard.vue` 历史模拟内容需持续补充真实指标（非本轮硬性阻塞）。

## 8. 异步任务模块
### 已完成
- Celery app、video task worker、PM2 管理。
- 状态查询、停止任务、结果回放链路。
- 超时/重试参数读取配置中心并应用任务状态机。
### 待完成
- 无（该模块本轮计划项已完成）。

## 9. 部署与运维模块
### 已完成
- GitHub Actions 自动部署。
- PM2 启动后端和 Celery worker。
- `.env.example` 补齐 Redis/Celery 配置。
- `predeploy_check` 发布前检查脚本已接入部署链路。
### 待完成
- GitHub Actions 部署 Secrets 实际配置与验证（`SERVER_HOST`、`SERVER_PORT`、`SERVER_USER`、`SERVER_PASSWORD`）。
- 在补齐 Secrets 后执行一次真实 push 触发部署并留存成功日志。

## 10. 测试与质量门禁模块
### 已完成
- 脚本回归集：`test_tracking_flow.py`、`test_stats_flow.py`、`test_environment_flow.py`、`test_review_notifications_flow.py`、`test_video_task_flow.py`。
- 新增 `test_admin_core_flow.py`，覆盖管理后台核心链路。
- CI 去除 `pytest ... || true`，失败阻断部署。
### 待完成
- 前端端到端自动化回归（浏览器层）可作为后续增强项。

---

## 当前结论
1. 你要求的业务闭环项（含用户/管理员检测分流、知识库 DB 化、QnA 错误态）已落地。
2. 阶段C全量门禁已通过，当前仅剩阶段D最后任务：GitHub Secrets 配置与一次成功 Actions 部署验证。
